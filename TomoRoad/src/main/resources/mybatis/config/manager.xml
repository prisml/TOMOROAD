<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="manager"> 	
    <sql id="selectMember">
		select id,password,name,sex,tel,profile from member
	</sql>
	<select id="managerLogin" resultType="managerVO" parameterType="managerVO">
 		select id,password from manager
 		where id=#{id} and password=#{password}
 	</select>
 	<select id="getMemberList" resultType="memberVO">
		select id,password,name,sex,tel from member
		order by id ASC
	</select>
    <update id="updateMemberByManager" parameterType="memberVO">
        update member set
		password=#{password}, name=#{name}, sex=#{sex}, tel=#{tel}
		where id=#{id}		
	</update>
	<delete id="deleteMemberByManager">
		delete from member where id=#{id}	
	</delete>
	<select id="getIdFromMember" resultType="memberVO">
		select id, password, name, sex, tel from member
		where id=#{id}
	</select>
	
	<select id="getStationListManager" resultType="stationvo">
		select name,detail,section,img,lat,lng,simple_detail as simpleDetail from station order by name ASC
	</select>
    <update id="updateStation" parameterType="stationVO">
        update station set
		simple_detail=#{simple_detail}, detail=#{detail}, section=#{section}, img=#{img}, lat=#{lat}, lng=#{lng}
		where name=#{name}		
	</update>
</mapper>














