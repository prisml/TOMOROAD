<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Sql Mapper -->
<mapper namespace="station">

	<sql id="selectStation">
		select name,detail from station
	</sql>

	<select id="getInfo" resultType="stationVO">
		<include refid="selectStation" />
		where name=#{value}
	</select>

	<select id="getKeyword" resultType="String">
		select name from station
		where name like '%' || #{value} || '%'
	</select>

	<select id="getList" resultType="stationVO">
		<include refid="selectStation" />
	</select>
</mapper>